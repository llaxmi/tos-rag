{
  "name": "tos-rag",
  "private": true,
  "packageManager": "pnpm@11.12.0",
  "scripts": {
    "dev": "turbo run dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "build": "turbo run build",
    "ingest": "pnpm --filter backend ingest",
    "build-questions": "pnpm --filter backend build-questions",
    "seed-questions": "pnpm --filter backend seed-questions",
    "run-phase1": "pnpm --filter backend run-phase1",
    "fetch-canonical": "pnpm --filter backend fetch-canonical"
  },
  "devDependencies": {
    "turbo": "^2.10.5"
  }
}
